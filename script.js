const skills = ["HTML", "CSS", "Javascript", "Python"];
 const skillsList = document.getElementById("skills-list");

 skills.forEach(skill) => {
    const li = DocumentFragment.createElement("li");
    li.textContent = skill;
    skillsList .appendChild
 }
